#include <iostream>
#include <vector>

using namespace std;
int main(){


    // to get number of elements in a vector, use .size(). 
    vector <int> my_list = {1,3,5,6};
    cout<< my_list.size();


    return 0;
}